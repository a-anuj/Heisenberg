# Keep this file in sync with pyproject.toml for docker and simple pip installs.

openenv-core[core]>=0.2.2
faker>=25.0.0
pydantic>=2.0.0
httpx>=0.27.0

fastapi>=0.100.0
uvicorn[standard]>=0.23.0

openai>=1.0.0
